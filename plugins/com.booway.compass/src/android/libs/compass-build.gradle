repositories{
    jcenter()
    flatDir{
        dirs 'libs'
    }
}

dependencies {
    compile 'androidx.appcompat:appcompat:1.3.1'
    compile "com.amap.api:3dmap:9.2.0"
    compile "com.amap.api:location:latest.integration"
}